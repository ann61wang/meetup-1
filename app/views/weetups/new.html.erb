<div class="col-md-4 col-md-offset-4">
    <h2>创建</h2>

    <hr>
    <%= form_for @weetup do |f| %>

    <% if @weetup.errors.any? %>
    <ul>
      <% @weetup.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
    <% end %>

    标题
    <%= f.text_field :title %>
    <br>
    叙述
    <br>
    <%= f.text_area :description %>
    <br>

    <%= f.submit "Submit", :disable_with => 'Submiting...' %>
    <% end %>

</div>
